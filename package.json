{
  "name": "taskflow-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build && node -e \"fs.cpSync('frontend/dist', 'dist', {recursive: true})\"",
    "preview": "npm --prefix frontend run preview"
  }
}
